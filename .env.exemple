# Copier en .env puis remplacer par une vraie valeur :
#   openssl rand -hex 16
APP_CLE=changez-moi

# uid/gid du compte qui fait tourner Syncthing sur le serveur (`id -u`, `id -g`).
# Doit correspondre, sinon Syncthing ne pourra plus écrire dans data/.
PUID=1000
PGID=1000
